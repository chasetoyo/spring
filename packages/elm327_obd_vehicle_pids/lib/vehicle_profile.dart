/// The make/model/year profile system used to look up a vehicle's
/// [PidSet] of custom PIDs — import this alongside a make-specific entry
/// point like `subaru.dart` or `toyota.dart`.
library;

export 'src/vehicle_profile.dart';
