name: ble_core
description: Vendor-neutral Bluetooth Low Energy plumbing shared by every device family in this workspace.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.8.1
  flutter: ">=1.17.0"
resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  flutter_blue_plus: ^2.3.10
  permission_handler: ^12.0.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
